- name: Debug FLY_API_TOKEN visibility
  run: |
    if [ -z "$FLY_API_TOKEN" ]; then
      echo "❌ FLY_API_TOKEN is NOT set"
      exit 1
    else
      echo "✅ FLY_API_TOKEN is set"
    fi
  env:
    FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
